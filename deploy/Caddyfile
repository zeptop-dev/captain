# Only for the reverse-proxy layout (docker-compose.proxy.yml or a bare-metal
# install with tls.auto off). Captain's default is to serve HTTPS itself.
panel.example.com {
	encode zstd gzip
	reverse_proxy 127.0.0.1:8080
}
